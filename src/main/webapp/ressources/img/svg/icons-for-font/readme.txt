To generate the font go to https://icomoon.io/app/#/select/font and load all svg.

The web application allows to generate a css and font files (ttf, eot, woff, svg). 
This will then be usable like "fontawesome".